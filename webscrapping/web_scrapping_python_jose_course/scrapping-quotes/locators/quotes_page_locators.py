class QuotesPageLocators:
    """[QUOTE = has the exactly quotation tag which will be used for extract all
    the information required like quote itself, author and tags related to the
    quote.]
    """
    QUOTE = 'div.quote'
